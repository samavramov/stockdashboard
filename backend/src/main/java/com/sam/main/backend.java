package com.sam.main;
/*package com.sam.stockDashboard;
import java.util.Map;
public class backend {
    public static void main(String[] args) {
        // Example usage of the aquireURL class
        String companyQuery = "Apple";
        Map<String, String> articles = aquireURL.getURL(companyQuery);
        
        // Print the fetched articles
        for (Map.Entry<String, String> entry : articles.entrySet()) {
            System.out.println("Title: " + entry.getKey());
            System.out.println("URL: " + entry.getValue());
            try{
                // Fetch and print the sentiment for each article
                //Double sentiment = aquireSentiment.getOllamaSentiment(entry.getValue(), companyQuery);
                //System.out.println("Sentiment: " + sentiment);
            } catch (Exception e) {
                System.out.println("Error fetching sentiment: " + e.getMessage());
            }
            System.out.println();
        }
    }
}*/
